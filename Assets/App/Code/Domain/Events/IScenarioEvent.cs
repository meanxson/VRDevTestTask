namespace App.Domain.Events
{
    /// <summary>
    /// Маркер доменного события сценария.
    /// Домен не публикует события сам и ничего не знает о шине: он возвращает их
    /// из методов сессии, а прикладной слой рассылает подписчикам. Благодаря этому
    /// правила прохождения остаются чистыми и полностью проверяемыми тестами.
    /// </summary>
    public interface IScenarioEvent
    {
    }
}
